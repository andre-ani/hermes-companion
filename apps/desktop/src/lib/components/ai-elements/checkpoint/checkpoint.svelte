<script lang="ts" module>
	import { cn, type WithElementRef } from "$lib/utils";
	import type { HTMLAttributes } from "svelte/elements";
	import type { Snippet } from "svelte";
	// indexing
	export interface CheckpointProps extends WithElementRef<HTMLAttributes<HTMLDivElement>> {
		children?: Snippet;
	}
</script>

<script lang="ts">
	import { Separator } from "$lib/components/ui/separator";

	let {
		class: className,
		children,
		ref = $bindable(null),
		...restProps
	}: CheckpointProps = $props();
</script>

<div
	bind:this={ref}
	class={cn("flex w-full min-w-0 items-center gap-0.5 overflow-clip text-muted-foreground", className)}
	{...restProps}
>
	{@render children?.()}
	<Separator />
</div>
